{
  "title": "CloudTrailIntegrationEnableResponse",
  "type": "object",
  "description": "Response returned when a disabled CloudTrail onboarding record is enabled again. The backend owns customer setup details and returns the AWS CloudFormation quick-create URL when needed.",
  "properties": {
    "integration": {
      "$ref": "./CloudTrailIntegration.schema.json"
    },
    "quickCreateUrl": {
      "type": "string",
      "format": "uri",
      "description": "AWS CloudFormation quick-create URL for the customer to open. Omitted when the integration was already enabled."
    }
  },
  "required": ["integration"],
  "additionalProperties": false
}
