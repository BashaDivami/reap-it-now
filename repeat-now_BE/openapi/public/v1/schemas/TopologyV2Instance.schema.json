{
  "title": "TopologyV2Instance",
  "description": "Instance within a domain. Conventionally encoded as `<kind>:<id>` (e.g., vlan:20, vrf:default, bgp:default, ospf:1). This is intentionally not an enum.",
  "type": "string",
  "examples": ["vlan:20", "vrf:default", "ospf:1"]
}

