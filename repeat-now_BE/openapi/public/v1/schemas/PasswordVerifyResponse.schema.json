{
  "title": "PasswordVerifyResponse",
  "description": "Result of verifying a password.",
  "type": "object",
  "properties": {
    "loginTx": { "type": "string", "description": "Short-lived, single-use handle to exchange at /auth/token (grantType: 'loginTx')." },
    "loginTxExpiresAt": { "type": "string", "format": "date-time", "nullable": true }
  },
  "required": ["loginTx"],
  "additionalProperties": false
}

