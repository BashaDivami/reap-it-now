{
  "title": "CredentialDetailsSnmpV2c",
  "type": "object",
  "description": "SNMP v2c communities.",
  "properties": {
    "port": { "type": "integer", "minimum": 1, "maximum": 65535, "default": 161, "description": "SNMP UDP port." },
    "readCommunitySecretId": {
      "type": "string", "format": "uuid",
      "description": "SecretId of type `snmp_community` for read operations."
    },
    "writeCommunitySecretId": {
      "type": "string", "format": "uuid",
      "description": "SecretId of type `snmp_community` for write operations (optional)."
    }
  },
  "required": ["readCommunitySecretId"],
  "additionalProperties": false
}

