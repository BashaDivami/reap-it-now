{
  "title": "ChartTimeBinding",
  "description": "Time policy for a chart metrics binding. fixed uses from/to/stepSec; rolling uses lookbackSeconds/stepSec. Services validate the mode-specific required fields because generated clients need a simple field-based model.",
  "type": "object",
  "properties": {
    "mode": {
      "type": "string",
      "enum": ["fixed", "rolling"],
      "description": "fixed uses absolute from/to; rolling uses a lookback window ending at now."
    },
    "from": {
      "type": "string",
      "format": "date-time",
      "description": "Start time for fixed charts."
    },
    "to": {
      "type": "string",
      "format": "date-time",
      "description": "End time for fixed charts."
    },
    "lookbackSeconds": {
      "type": "integer",
      "minimum": 1,
      "description": "Rolling lookback duration in seconds."
    },
    "stepSec": {
      "type": "integer",
      "minimum": 1,
      "description": "Range-query step size in seconds."
    }
  },
  "required": ["mode", "stepSec"],
  "additionalProperties": false
}
