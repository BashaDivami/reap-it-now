{
  "title": "SecretUpdate",
  "description": "Partial update for non-sensitive fields. Secret value cannot be changed here; use POST /value.",
  "type": "object",
  "properties": {
    "metadata": {
      "type": "object",
      "additionalProperties": true,
      "description": "Replace or merge non-sensitive annotations."
    }
  },
  "additionalProperties": false
}

