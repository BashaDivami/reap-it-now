{
  "title": "Secret",
  "description": "Opaque secret material metadata. The secret value is never returned by GET. Use POST /value to set/rotate.",
  "allOf": [
    { "$ref": "./ResourceBase.schema.json" },
    { "$ref": "./OrgScoped.schema.json" },
    {
      "type": "object",
      "properties": {
        "type": { "$ref": "./SecretType.schema.json" },
        "fingerprint": {
          "type": "string",
          "description": "SHA-256 hex digest of the normalized secret bytes (or a representative component for bundles)."
        },
        "last4": {
          "type": "string",
          "description": "Last 4 printable characters for human confirmation; omitted for purely binary values."
        },
        "metadata": {
          "type": "object",
          "additionalProperties": true,
          "description": "Non-sensitive annotations (e.g., label, owner hint)."
        }
      },
      "required": ["type"],
      "additionalProperties": false
    }
  ]
}

