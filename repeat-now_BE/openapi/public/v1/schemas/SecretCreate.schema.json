{
  "title": "SecretCreate",
  "description": "Create a secret. The value is accepted here but is never returned by GET.",
  "type": "object",
  "properties": {
    "type": { "$ref": "./SecretType.schema.json" },
    "value": {
      "type": "string",
      "description": "Secret material (string). PEM/PKCS#12 may be provided as literal PEM or base64 as appropriate."
    },
    "metadata": {
      "type": "object",
      "additionalProperties": true,
      "description": "Optional non-sensitive annotations."
    }
  },
  "required": ["type", "value"],
  "additionalProperties": false
}

