{
  "title": "SecretSetValue",
  "description": "Set/rotate secret material for an existing secret.",
  "type": "object",
  "properties": {
    "value": {
      "type": "string",
      "description": "New secret material."
    },
    "reason": {
      "type": "string",
      "description": "Optional human-readable rotation reason (for audit)."
    }
  },
  "required": ["value"],
  "additionalProperties": false
}
