{
  "title": "CredentialCreateSnmpV2c",
  "description": "Create a SNMP v2c credential.",
  "type": "object",
  "properties": {
    "name": { "type": "string", "description": "Display label." },
    "description": { "type": "string", "nullable": true },
    "labels": {
      "type": "object",
      "additionalProperties": { "type": "string" },
      "description": "Optional labels."
    },
    "kind": { "type": "string", "enum": ["snmp_v2c"] },
    "details": { "$ref": "./CredentialDetailsSnmpV2c.schema.json" }
  },
  "required": ["kind", "details"],
  "additionalProperties": false,
  "x-discriminator-value": "snmp_v2c"
}
