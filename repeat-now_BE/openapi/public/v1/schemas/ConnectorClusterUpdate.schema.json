{
  "title": "ConnectorClusterUpdate",
  "description": "Partial update for a connector cluster. Omitted fields are unchanged.",
  "type": "object",
  "properties": {
    "name": { "type": "string", "description": "New display name." },
    "description": { "type": "string", "nullable": true, "description": "Admin notes." },
    "labels": {
      "type": "object",
      "description": "Replace all labels with the provided set.",
      "additionalProperties": { "type": "string" }
    },
    "settings": {
      "$ref": "./ConnectorClusterSettings.schema.json",
      "description": "Merge in runtime settings. Only include the knobs you intend to change."
    }
  },
  "additionalProperties": false
}
