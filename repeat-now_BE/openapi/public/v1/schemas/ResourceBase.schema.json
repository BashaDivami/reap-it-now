{
  "title": "ResourceBase",
  "description": "Common server-managed fields for persisted resources.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Resource UUID generated by the backend.",
      "readOnly": true
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "Creation timestamp (UTC, RFC 3339).",
      "readOnly": true
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Last update timestamp (UTC, RFC 3339).",
      "readOnly": true
    },
    "createdBy": {
      "type": "string",
      "format": "uuid",
      "description": "User id of the creator (or system id for system actions).",
      "readOnly": true
    },
    "updatedBy": {
      "type": "string",
      "format": "uuid",
      "description": "User id of the last editor (or system id for system actions).",
      "readOnly": true
    }
  },
  "additionalProperties": false
}
