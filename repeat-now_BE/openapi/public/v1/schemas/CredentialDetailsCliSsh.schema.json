{
  "title": "CredentialDetailsCliSsh",
  "type": "object",
  "description": "CLI over SSH. Auth can be password or SSH key. Optional enable password.",
  "properties": {
    "username": { "type": "string", "description": "Device login user." },
    "port": { "type": "integer", "minimum": 1, "maximum": 65535, "default": 22, "description": "SSH port." },
    "execPasswordSecretId": {
      "type": "string", "format": "uuid",
      "description": "SecretId of type `password` for CLI login."
    },
    "enablePasswordSecretId": {
      "type": "string", "format": "uuid",
      "description": "SecretId of type `enable_password` for privilege escalation."
    },
    "sshPrivateKeySecretId": {
      "type": "string", "format": "uuid",
      "description": "SecretId of type `ssh_private_key` (PEM)."
    },
    "sshPassphraseSecretId": {
      "type": "string", "format": "uuid",
      "description": "SecretId of type `ssh_passphrase` (optional)."
    },
    "cyberarkStoreId": {
      "type": "string",
      "format": "uuid",
      "description": "Credential store ID for CyberArk CCP."
    }
  },
  "required": ["username"],
  "additionalProperties": false
}
