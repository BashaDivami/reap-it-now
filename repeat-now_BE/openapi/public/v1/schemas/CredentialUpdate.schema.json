{
  "title": "CredentialUpdate",
  "description": "Partial update. `kind` cannot change after creation. If `details` is provided, it must conform to the existing kind.",
  "type": "object",
  "properties": {
    "name": { "type": "string" },
    "description": { "type": "string", "nullable": true },
    "labels": { "type": "object", "additionalProperties": { "type": "string" } },
    "details": {
      "description": "Provide only fields relevant to the existing `kind`.",
      "oneOf": [
        { "$ref": "./CredentialDetailsCliSsh.schema.json" },
        { "$ref": "./CredentialDetailsSnmpV2c.schema.json" },
        { "$ref": "./CredentialDetailsSnmpV3.schema.json" },
        { "$ref": "./CredentialDetailsApiRest.schema.json" }
      ]
    }
  },
  "additionalProperties": false
}
