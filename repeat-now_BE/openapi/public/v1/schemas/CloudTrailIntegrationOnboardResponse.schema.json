{
  "title": "CloudTrailIntegrationOnboardResponse",
  "type": "object",
  "description": "Response returned when CloudTrail onboarding is created. The backend owns customer setup details and returns the AWS CloudFormation quick-create URL.",
  "properties": {
    "integration": {
      "$ref": "./CloudTrailIntegration.schema.json"
    },
    "quickCreateUrl": {
      "type": "string",
      "format": "uri",
      "description": "AWS CloudFormation quick-create URL for the customer to open."
    }
  },
  "required": ["integration", "quickCreateUrl"],
  "additionalProperties": false
}
