{
  "title": "PasswordResetRequest",
  "description": "Reset an existing password using email ownership proven by emailTx.",
  "type": "object",
  "properties": {
    "emailTx": { "type": "string" },
    "newPassword": { "type": "string" }
  },
  "required": ["emailTx", "newPassword"],
  "additionalProperties": false
}

