{
  "title": "PasswordRegisterRequest",
  "description": "Set an initial password for a user proven by a valid emailTx.",
  "type": "object",
  "properties": {
    "emailTx": { "type": "string", "description": "Short-lived email handle from /auth/discover/verify or /auth/invitations/accept." },
    "password": { "type": "string", "description": "Clear-text password over HTTPS; server hashes & stores." }
  },
  "required": ["emailTx", "password"],
  "additionalProperties": false
}

