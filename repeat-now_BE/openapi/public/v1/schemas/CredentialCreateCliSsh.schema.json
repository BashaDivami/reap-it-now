{
  "title": "CredentialCreateCliSsh",
  "description": "Create a CLI (SSH) credential.",
  "type": "object",
  "properties": {
    "name": { "type": "string", "description": "Display label." },
    "description": { "type": "string", "nullable": true },
    "labels": {
      "type": "object",
      "additionalProperties": { "type": "string" },
      "description": "Optional labels."
    },
    "kind": { "type": "string", "enum": ["cli_ssh"] },
    "details": { "$ref": "./CredentialDetailsCliSsh.schema.json" }
  },
  "required": ["kind", "details"],
  "additionalProperties": false,
  "x-discriminator-value": "cli_ssh"
}
